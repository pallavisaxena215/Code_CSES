#include<iostream>
using namespace std;
bool anagram(string str1, string str2)
{ int l1=strlen(str1);
   int l2=strlen(str2);
   
   if(l1!=l2)
   return false;
   sort(str1);
   sort(str2);
   for(int i=0;i<n;i++)
   {
   if(str1[i]!=str2[i])
   { return false;
   }
   return true;
  }
 
 int main(){
 string s1="geeks";
 string s2="keges";
 if(anagram(s1,s2))
 cout<<"ANAGRAM";
 else
 cout<<"NOT ANAGRAMS";
 
 return 0;
}
